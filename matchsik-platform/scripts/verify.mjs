import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),dist=path.join(root,'frontend','dist');const req=['index.html','admin.html','my.html','student.html','teacher.html','config.js','_headers','_redirects','site.webmanifest'];const missing=req.filter(x=>!fs.existsSync(path.join(dist,x)));if(missing.length)throw new Error('Missing: '+missing.join(', '));for(const html of req.filter(x=>x.endsWith('.html'))){const text=fs.readFileSync(path.join(dist,html),'utf8');for(const m of text.matchAll(/(?:src|href)="(\/[^"?#]+)/g)){const p=path.join(dist,m[1].slice(1));if(!fs.existsSync(p))throw new Error(`${html}: missing ${m[1]}`)}}console.log('Static references verified.');