# GitHub 웹 업로드

1. `fruitmarket-production.zip`을 PC에서 압축 해제합니다.
2. GitHub에서 `fruitmarket-production` 이름의 Private 빈 저장소를 만듭니다.
3. 압축 해제한 폴더 안의 파일과 폴더 전체를 `Add file → Upload files`로 올립니다.
4. 저장소 첫 화면에서 `package.json`, `netlify.toml`, `site`, `supabase`가 바로 보여야 합니다.
